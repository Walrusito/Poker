# Nota: torch/torchvision NO se instalan aquí si usas la imagen Docker oficial
# pytorch/pytorch:2.3.0-cuda12.1 — ya incluye torch 2.3 con CUDA.
# Si corres en local (sin Docker), descomenta la línea de torch.

# torch>=2.3.0,<3.0   # descomentar si instalas en local sin imagen pytorch

numpy>=1.26.0,<2.0
mlflow>=2.12.0,<3.0
tqdm>=4.66.0,<5.0
